[www]
; Podman rootless maps container root to host user.
; Run php-fpm workers as root so they can write to
; storage/framework/views when compiling Blade components.
user = root
group = root
listen.owner = root
listen.group = root
