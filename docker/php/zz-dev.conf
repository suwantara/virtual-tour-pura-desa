[www]
; Podman rootless maps container users to host user.
; Use www-data (default) — permissions handled in entrypoint.
user = www-data
group = www-data
listen.owner = www-data
listen.group = www-data
